#include <stdio.h>

int main (void)
{
char name[] = "Ruth Claire";
i = sizeof(i);
//f = sizeof(f );
int i = 7;
printf("The size of i is %d.\n", sizeof(i));
printf("The size of name is %d.\n", sizeof(name));
}