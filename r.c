#include <stdio.h>

int factorial_recursion (int num) {
    if (num == 0 || num == 1) {
        return 1;
    }
    
}
int factorial_recursion(int num) { 
    
    int factorial_iteratrive(int num) {
        int i ;
        for (i = num; i > 1; i--) {
            fact *= i;
        }
        return fact;
    }
int main (void)
{
  int value, fact;
  
  printf("Enter the number to find the factorial of" 
  );
  scanf("%d", &value);
  fact = factorial)
}